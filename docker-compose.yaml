version: "3.8"
services:
  backend:
    container_name: mancala-backend
    build: ./
    image: jurengis/mancala-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}